#ifndef BUFFER_HPP
#define BUFFER_HPP

#include "BaseElement.hpp"

class Buffer: public BaseElement
{
public:
  Buffer(/* args */);
  ~Buffer() = default;
private:
  /* data */
};

#endif // !BUFFER_HPP
